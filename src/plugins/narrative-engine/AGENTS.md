# narrative-engine

Não mexer em `findMatchingRule` salvo pedido explícito de match.
Verbos DO novos: `EffectOp` + `RuleEffects.register` no plugin dono, não em `applyChanges`.
Ler `CONTEXT.md` desta pasta antes de `lib/`.
