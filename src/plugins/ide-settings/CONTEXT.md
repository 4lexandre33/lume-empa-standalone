# lume-ide-settings

Preferências da IDE (layout, onboarding).

## Abrir
- `index.ts` / `types.ts`

## Provides
IdeSettingsService

## Fora de âmbito
`project.settings.tree` (pastas) → `ide-state`, não aqui.
